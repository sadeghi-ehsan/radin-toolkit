import { Badge as SBTBadge, BadgeColors } from "../../src";

const Badge = () => (
  <div className="flex mb-2">
    <SBTBadge color="dark-success">+ 200</SBTBadge>
  </div>
);

export default Badge;
