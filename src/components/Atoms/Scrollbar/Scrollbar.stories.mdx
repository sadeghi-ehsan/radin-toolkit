
import { Meta } from '@storybook/addon-docs';

<Meta title="COMPONENTS/Atoms/Scrollbar/Docs" />





### توضیحات :

اسکرول بار یک html class name میباشد که با افزودن کلاس ‍`scrollbar-primary` به یک المنت ، اسکرول بار آن را استایل دهی میکند.

### نمونه:
```html
<div className="scrollbar-primary"></div>
```

